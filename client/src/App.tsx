import Button from "./component/Button"

function App() {

  return (
    <div>

      <Button variant="primary" size="sm" onclick={() => {}} text="Add" />
    
      <Button variant="secondary" size="md" onclick={() => {}} text="Add" />
    </div>
  )
}

export default App
